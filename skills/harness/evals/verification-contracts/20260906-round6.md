# 第 6 次 fresh-context 驗收契約 — 2b：文件整合、正式 checker 接線與漏檢修正

你是一個 fresh-context 驗收者。請對下列產出物做獨立、唯讀驗收。
你沒有參與實作，不要索取實作者的推理過程，也不要進入修復循環。

Repo：`/Users/tinywu/Desktop/Apps/tiny-agents-skills`
HEAD：`b9cb8150a30be2b69b07a615e02f5ed8b9f21d2b`（2b 未 commit，HEAD 仍停在 2b 之前）

working tree 兩個數字**分開記，不要混用**：

* **pre-2b＝52 筆**（第 4 次驗收於同一 HEAD 實測）
* **現況＝55 筆**：52 ＋ `VERIFICATION-LOG.md` ＋ `verification-records/` ＋ `verification-contracts/`

不得清理、不得 commit、不得 `git add`。

**本輪的由來。** 第 5 次於第 7 條 FAIL（round-4 record 的 canonicalization 自述與
實際 digest 不符），修正後重跑；重跑於第 2 條 FAIL——`ledger_check.py` 對 narrative 區
大量漏檢（實測在 gate 區翻掉一筆 disposition、塞入不存在的 ID、補一條錯算式，
全部回 `rc=0`）。兩次修正都動到被驗物本身，**因此本輪重跑全部條件，
不得沿用前兩輪的任何逐條 PASS**。前一版契約 `20260906-round5.md` 保留為歷史。

**本檔即驗收契約本身。** 契約無法把自己的 hash 寫在自己裡面（寫進去就改變了該 hash），
但它**可以也應該被外部鎖定**：啟動本次驗收的 prompt 會給出本檔的 SHA-256，
請先比對再開始。**你不需要、也不得把任何結果寫進 repo**——archive record 由
implementation session 在收到你的報告後建立。

## 1. 版本鎖定

任何一項不符即停止並回報。三個區塊的**證據力不同，不可互相代替**。

### A. 2b＋digest 修正批次的產出物（6）— 現值，用於確認你讀到的是同一份產出物

```
a8b62bfdf097819b88ddbcff177a238df9a3761c8ba770814a06f9fe1f865379  skills/harness/evals/KNOWN-ISSUES.md
4bffd036eca25019d93361b510d2c7e008e76f5dc7ec5973694de8b620178b25  skills/harness/evals/STATUS.md
8c796f3f4b0d9b56229b0d0c9067800c7f2729f72d9922ade762476e244af368  skills/harness/evals/RECHECK-20260904-supersedes-closure.md
9259374b885185defe66eb87eeb8d458eb6f792765309f405b636d8b19267fd5  skills/harness/evals/VERIFICATION-LOG.md
734d37aa5f4248c9577d9b4372fc73c862887b16340c2fde093e31f134b236f6  skills/harness/evals/verification-records/20260905-round4-460e5a01bfd601ee.md
c1448405a5da39d5637ae9f9bda42a15c496befa9ad11651e6ccc47b9906cd3d  skills/harness/evals/verification-records/20260905-round4-f79e0b389b2455b0.md
```

**兩份 round-4 record 的角色不同，不可互換。**

* `…-460e5a01bfd601ee.md` — **有效**，第 4 次的 gate 依據。
* `…-f79e0b389b2455b0.md` — **superseded，不得作為 gate 依據**，但**必須逐 byte 保持不變**。
  它記載的是同一次驗收、結論無誤；失效的只有身分綁定——digest 以裸檔名計算而自述為
  相對路徑。它自己宣告不可覆寫，因此以新增取代，不修改、不刪除。
  上列 `c1448405…` 是它在第 5 次驗收時的值，屬**有 baseline 的未動項**：若不相符即為 FAIL。

**canonical digest 規則**（第 5 次驗收 FAIL 後於 2026-09-06 固定）：
每列 `<sha256>` ＋兩個空格＋**完整 repo-relative path**，依該紀錄第 1 節表列順序，
以 `\n` 連接，**無尾換行**；檔名取前 16 hex。

### B. 具 pre-2b baseline 的未動項 — **可據以判斷「2b 沒動它」**

來源：第 4 次驗收契約的鎖定值，與第 4 次 verifier 於 2b 之前現算並記錄的值。

```
2eb828e2f112b77164d282c85a4bbeaa00719e8f57e44298270c176746cbf18b  skills/harness/evals/CLOSURE-20260904-disposition.md
d391d6573a1a9a925c09a32aa9f9194f2fbe80c68df58d2d0bd5a5c3b7c3bf57  skills/harness/evals/DE-01-RESULTS-20260904.md
e86c4fa1ff4259f69e514a7aff8e58dfaee048c97a176dfbdea372fe28611f8c  skills/harness/evals/DE-01-PREFLIGHT.md
10a88e48388811d5bc957055ee960ae81b92fecc5bd3faaaeabdadd1ac36439e  skills/discipline/judgment/SKILL.md
8a6d99f4279db9b091f3fdd8ee41721883f50eec08d1ea72e5b578c5fbc9f109  skills/discipline/judgment/evals/fixtures.json
```

八個 identity domain 的九輪綁定值（`hash_schema v6`）：

```
dispatch_skill         2d6c0d99b1c4ca927e2c06c82e065c389fa6a6b8ddbc325b0e9bde9bc071c4fc
judgment_skill  arm A  6e96c802be26d299a827bd93ed62ca576159444332c1ae641ac547e0ed9208d1
judgment_skill  arm B  a26b436888d81e7181199932590ca29f21e58ff9b8066eb792458e68ed5b3d47
token_preflight_skill  be72017e9fc7952e9a403cf90e951669496b08f4339938bc73a2829d44ef592a
evaluator              65bc6c07d058697cda34045637d3223568cf09626cc38b8385bac46d41157480
runner                 005ed2f71ac49a8b6747dd1c43f42c8ee67315a9fb3ec8ce395f921ccf6cee3c
fixtures        DE-01  7a1b54c1eddf710e030323ab6124e525d983ef5b32a1316d1780dd12d08d4738
fixtures        KI-07  dbbb4786db55bf8f568a5071f013072edb68d78273474ee49744a1654639656a
execution_context      e7beb6eded81b04fae8bee2076dec7ceeefc5f55f60980aebd3b7fc4cfb9d4e0
evaluation_context     64996161082a8b43374e911c1b5e1d2c4ba36333cd324c53cfc37caecdc2b33c
```

`runs/` 內不受 trace manifest 涵蓋的 raw（第 4 次驗收於 2b 之前現算）：

```
27 檔  runs/{de01,ki07}-arm*/raw/*.judge.json  281f739d1b6d27ffaf32a83f9e82dd0b16acc63c91a6803ad92f3e83b355adcd
36 檔  runs/{de01,ki07}-arm*/raw/*.err         9b003f7eb27be0d8f49836d1e92febffe70fc2de34baf3e73a2c7b8463ab78f5
彙總方式：find … | sort | xargs shasum -a 256 | shasum -a 256
```

pre-2b 的 ledger 逐列 canonical projection（第 4 次驗收自 `KNOWN-ISSUES.md@0c7bc8cc…`
機械抽取）。格式：每列 `KI-nn<TAB>disposition<TAB>action`，依 ID 排序，行尾換行。

```
KI-01 FIX_FIXTURE DONE          KI-02 FIX_SKILL NOT_DONE        KI-03 ACCEPT_AS_KNOWN N/A
KI-04 FIX_FIXTURE DONE          KI-05 ACCEPT_AS_KNOWN N/A       KI-06 FIX_FIXTURE NOT_DONE
KI-07 FIX_SKILL DONE            KI-09 FIX_SKILL NOT_DONE        KI-10 FIX_FIXTURE NOT_DONE
KI-11 NEEDS_EVIDENCE N/A        KI-12 NEEDS_EVIDENCE N/A        KI-13 FIX_FIXTURE NOT_DONE
KI-14 FIX_SKILL NOT_DONE        KI-15 FIX_SKILL NOT_DONE        KI-16 NEEDS_EVIDENCE N/A
KI-17 FIX_FIXTURE NOT_DONE      KI-18 NEEDS_EVIDENCE N/A        KI-19 FIX_FIXTURE NOT_DONE
KI-20 FIX_FIXTURE NOT_DONE      KI-21 FIX_SKILL NOT_DONE
projection digest  aff449be31cc3269a2ccc68af714c72fff08d5c012d793bc100eec15ea55dc9c
```

⚠ **此 projection 是轉錄值**：來源檔已被 2b 覆寫，你無法從 `0c7bc8cc…` 重新導出它。
它足以判斷「現況是否等於這張表」，但「這張表是否忠實反映當時的檔案」不在本次可驗範圍，
已計入第 18 條。

### C. 僅有 post-2b 現值、**無** pre-2b baseline — 不得據以宣稱「2b 沒動它」

```
3e3d7cf11bbd61b6f88e519dab8787f7ce5b63788b7f56500ccb1e8a102c095d  scripts/ledger_check.py
ab1ad8f97e5f00d8d5f6e4059ae95edaa708fe165c066a0d42408f74e70fa6ab  scripts/run_domain_check.py
6dac50ec4d2f487ce33a979a68e5be92e1691a50dea1b48ce404f192c11211e4  scripts/ledger_check_selftest.py
774a0d7ff0722d6c4ed6838a61f1e6ab82b2b6bb4f742fcba5e8bb57435cb89b  scripts/ledger_check_mutants.py
c15a80fdcbcc3eaefd3aaeff11e931f21269564e3cd0a7ef3673cb9af872620e  scripts/LEDGER-CHECK-SPEC.md
6f2e11b4f54d03dd6bfa81c95a7a3ec8ef79c11e4723853e5027b70ab457dae8  skills/harness/evals/de01/run-one.sh
62bfe6df42f0c39f99b8ceb72d48ff8abf4869be740318f3e64e473bfcf616ba  skills/harness/evals/judge_selftest.py
ae9ecfddd9f537912a300d06e73ae1794161c3e013753c49cdab7907e4829d50  skills/harness/evals/score_selftest.py
5a1b8b06a672099744ba372a47e31e260cd3d2c141d74071dc40e208944d3ffa  skills/harness/evals/record_selftest.py
04f2785ce446607e1c50c74de76b4c5c663079fda9db7e01b795cb2216d9828c  skills/harness/evals/transport_selftest.py
f38a513e2d5c19e4fbf57cae39078aa8f62af53eb360108d77e3f1638913a517  skills/harness/evals/acceptance-r2-regression.py
```

這 11 項只確認現值相符，**不得據以宣稱未動**。其中 `ledger_check.py`／selftest／
mutants／`LEDGER-CHECK-SPEC.md` 四支在本批次**確實被修改過**（漏檢修正），
與 `run_domain_check.py` 及其餘六項的未動狀態不同——後者有第 5 次驗收的
pre-fix 值可對照，前者沒有。

## 2. 驗收條件

逐條給 PASS／FAIL／UNVERIFIABLE。**每條只能有單一 verdict。**

1. 九個 marker 區各恰好出現一次、BEGIN 在 END 之前：`KNOWN-ISSUES.md` 八個
   （`ledger:summary`／`active`／`retired`／`distribution`／`gate-released`／
   `gate-blocked`／`gate-totals`／`freeze`），`STATUS.md` 一個（`status:current-summary`
   ——**注意此區前綴為 `status:` 而非 `ledger:`**）。`ledger:gate-released` 與
   `ledger:gate-blocked` 是否各有唯一一行 `IDs:`。名稱是否與 `LEDGER-CHECK-SPEC.md` 一致。
2. `ledger_check.py` 對正式文件的結果。**不要只看 exit code**：請在 repo 外複本上
   注入缺陷並確認每一種都被抓到、且命中預期的 error code。**至少涵蓋前一輪實測
   漏掉的六種形式**——(a) 在 gate 區補一張表格並於其中翻掉一筆 disposition；
   (b) 在 gate 散文塞入不存在的 `KI-99`；(c) 在 `gate-totals` 補第二條錯算式；
   (d) 把 `gate-released` 的「N 筆」改成錯值；(e) 在 `status:current-summary`
   以中文數字寫筆數；(f) 在該區補一個未受檢的「N 筆」。
   另需確認 **marker 區外的歷史數字不被誤判**（兩份文件都留有大量舊數字）。
   前一輪這六種全部回 `rc=0`；若你的注入仍有任一種回 `rc=0`，即為 FAIL。
3. `run_domain_check.py` 以**九份指定 run、八個 required domain、`hash_schema v6`、
   不使用 `--allow-prefix`** 執行的結果。同樣請注入缺陷驗證其 PASS 有意義。
4. freeze 表：是否**一個值一列**、綁定值欄整格為**完整 64 位 hex**、每列列出綁定輪次。
   請**自行從九份 run record 重算** domain→值→輪次的對應，與表格逐格比對，
   並確認多值 domain 的每個值都完整列出。
5. 獨立重算 ledger：active 列數、disposition 與 action status 分佈、
   個別放行與仍擋住兩個集合，與文件宣稱逐格比對；兩集合須互斥且覆蓋全部 active 列。
6. `RECHECK-20260904-supersedes-closure.md` 的**當前狀態**（不涉及與前版比對）：
   (a) 是否已不含自身的驗收歷程與狀態敘述（「尚缺第 N 次」「驗收歷程」「已修正，待獨立驗收」
   之類）；(b) 其判定章節是否仍具備下列 evidence anchors——W8／W9／W11／W12 四格改判、
   `.required[0]`／`.required[1]` 的逐輪 verdict 表、`FAIL > UNVERIFIABLE > PASS` 的
   優先序結論、§6 的 provenance 限制與兩個 raw 指紋。缺任一項為 FAIL。
7. **有效 record**（`…-460e5a01bfd601ee.md`）：
   (a) 檔名中的 16 hex 是否可由該檔第 1 節的三個 subject hash、依該檔 §0 自述的
   canonical 規則**實際重算得出**——請自己算，不要採信自述；
   (b) §0 所列三個變體值是否都能重現，且標為 canonical 的那一個確與檔名一致；
   (c) 是否包含 subject hash、HEAD、驗收契約 hash、正式 verdict 與門檻結果、
   verifier 身分、verifier session／task ID；
   (d) **取不到的欄位是否明白寫出「無法證明 freshness」而非省略。**
   任一子項不成立即為 FAIL。
8. **取代關係**：有效 record 是否明示取代舊 record、說明失效原因（canonicalization
   自述與實際 digest 不符）、並聲明舊檔保留不修改；兩份是否都明示**由實作者轉錄、
   只算 archive、不因落檔而取得獨立 provenance**。
   舊 record `…-f79e0b389b2455b0.md` 是否**逐 byte 未變**（＝ `c1448405…`）。
9. `VERIFICATION-LOG.md`：(a) 是否明示自己**只是索引、不得作為 gate 的唯一依據**；
   (b) 前三輪「無不可覆寫紀錄」是否據實標示，而非以摘要冒充紀錄；
   (c) 是否把舊 record 標為 superseded／不得作為 gate 依據，且第 4 次那一列的連結
   **指向有效 record**；(d) canonical digest 規則是否已寫入本檔而非只留一句話；
   (e) 2b 的第 5 次 FAIL 是否據實記載。
10. **Owner disposition 與 gate 未被更動**：把 `ledger:active` 區抽成 §1-B 的 canonical
    projection 格式，與該處所列 20 列**逐列**比對，並重算其 digest 是否等於
    `aff449be…`；`ledger:gate-totals` 是否仍為 active 20、放行 5、阻擋 15、gate 關閉。
    有任何一列改變即為 FAIL——2b 的授權範圍不含裁決。
11. §1-B 五個檔案 hash 是否相符（2a 交付物與 runtime 文件未動）。
12. runtime 與證據未動，**只驗有 baseline 的部分**：
    (a) §1-B 的八個 identity domain 現算是否等於九輪綁定（含重建 `execution_context`
    與 `evaluation_context`）；
    (b) 九份 `trace-manifest.json` 所列 72 個檔案的 sha256 是否逐一相符；
    (c) 兩個 raw 指紋（27 檔／36 檔）是否重算相符；
    (d) `de01/armA.SKILL.md` 是否仍受九輪 arm A 綁定所釘住——把
    `skills/discipline/judgment/` 複製到 repo 外，以 `armA.SKILL.md` 取代其 `SKILL.md`，
    `bundle-hash.sh skill` 現算應等於 `6e96c802…`。
    §1-C 的 11 項不屬本條，見第 18 條。
13. §1-C 的 11 項現值是否與所列 hash 相符（本輪只確認現值，不宣稱未動）。
14. 重跑 `ledger_check_selftest.py` 與 `ledger_check_mutants.py`：案例數、mutant 數、
    `classify()` 自檢、error code 覆蓋、baseline 邊界，是否都如其輸出所述。
    **並確認新增的 narrative guard 每一支都有能殺掉它的 mutant**——停用任一支
    檢查後必須有案例失敗；若某支停用後零案例失敗，那支就是沒有被覆蓋。
15. 四支 Python 檔在 **repo 外複本**編譯是否通過（避免在 repo 內產生 `__pycache__`）。
16. **marker 外是否新增了現值宣稱。** checker 只解析 marker 內（刻意如此，否則歷史段
    的正確數字會讓錯值矇混過關），因此把宣稱搬到 marker 外依構造偵測不到。
    請人工掃 `KNOWN-ISSUES.md` 與 `STATUS.md`，確認本批次刪掉的重複現值
    （放行／阻擋的筆數、集合、分類表）**沒有被搬到 marker 外**繼續存在。
    這一條無法機械化，請明確寫出你掃了哪些範圍。
17. 副作用：`git status` 筆數是否為 **55**、暫存區是否為空、HEAD 是否仍為 `b9cb8150`；
    相對 pre-2b 的 52 筆，新增是否**恰為三項**——`VERIFICATION-LOG.md`、
    `verification-records/`、`verification-contracts/`（第三項即本契約所在目錄）。
18. **無 pre-2b baseline 的部分，一律 UNVERIFIABLE，不得以文件自述或內容主題推定補足。**
    已知落在本條的有四類：(a) repo 內無 2b 的 pre-cycle snapshot，「本批只改了哪些 byte」
    不可重建；(b) §1-C 的 11 項無 pre-2b 值；(c) §1-B 的 ledger projection 是轉錄值，
    其對當時檔案的忠實度不可重建；(d) `.judge.json` 與 `.err` 不在任何 contemporaneous
    manifest 內，兩個指紋只證明現況、不證明產出後未被改動。
    本條**預期即為 UNVERIFIABLE**。若你發現第五類，一併寫在本條之下。

## 3. 允許的零成本驗證

唯讀查詢、SHA-256、`jq`、`rg`、`git status/diff`（不得 `git add`／`commit`／`push`），以及：

```
PYTHONDONTWRITEBYTECODE=1 python3 scripts/ledger_check.py --known-issues <path> --status <path>
PYTHONDONTWRITEBYTECODE=1 python3 scripts/run_domain_check.py --known-issues <path> --hash-schema v6 --required-domain <d>… --run <dir>…
PYTHONDONTWRITEBYTECODE=1 python3 scripts/ledger_check_selftest.py
PYTHONDONTWRITEBYTECODE=1 python3 scripts/ledger_check_mutants.py
python3 -m py_compile <repo 外複本>
sh scripts/bundle-hash.sh …
PYTHONDONTWRITEBYTECODE=1 python3 skills/harness/evals/judge.py --print-context
HARNESS_RULES_FILE=<path> HARNESS_SETTINGS_FILE=<path> sh skills/harness/evals/run-fixture.sh --print-context
```

條件 2、3、12(d)、15 的注入、重建與複本一律在 **repo 之外**進行。`--print-context` 不起 session。

## 4. 硬限制

* 唯讀，**不修改 repo 內任何檔案**，包含不寫 `verification-records/`。
  archive record 由 implementation session 事後建立。
* 不 commit、push、發布、清理 working tree、更新 installed skill。
* 不執行任何付費 session（非 `--dry-run` 的 suite、`run-one.sh`、subject、judge）。
* 不改 Owner disposition 或 release gate。
* **每條只能有單一 verdict**；含無法驗證的子項時該條整條判 `UNVERIFIABLE`。
* 整體判定優先序：**FAIL > UNVERIFIABLE > PASS**。
* **完成門檻**：第 18 條先驗即為 `UNVERIFIABLE`，故整體**不可能是 `PASS`**。
  門檻是「**無 FAIL，且 `UNVERIFIABLE` 僅限第 18 條**」。達到門檻時整體正式 verdict
  仍記為 `UNVERIFIABLE`，**不得寫成 `PASS`**；請在首行明確寫出「達到門檻／未達門檻」。
* 報告不超過 40 個 Markdown source lines。
* 先報整體判定，再逐條列證據；FAIL 只附 minimal fix 建議，回報後結束。
* **請在報告末尾附上你自己的 session／task ID；取不到就明白寫出「unavailable」。**
  **不要**嘗試計算自己回覆內容的 digest——那是自我指涉，完整輸出尚未產生時無法計算。
  原始輸出 digest 由呼叫端在收到回覆後計算；取不到 exact raw bytes 即記為 unavailable。
