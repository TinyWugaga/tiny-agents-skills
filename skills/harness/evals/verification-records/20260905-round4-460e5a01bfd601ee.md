# 驗收紀錄 — 文件批次 第 4 次 fresh-context 驗收（correction record）

**本檔不可覆寫。** 檔名內的 `460e5a01bfd601ee` 是被驗三檔 SHA-256 的 canonical digest，
內容與身分因此綁定：任何一個 subject 檔變動，下一輪就是另一份紀錄，不是修改本檔。

**本檔取代 [`20260905-round4-f79e0b389b2455b0.md`](20260905-round4-f79e0b389b2455b0.md)。**
該檔記載的是**同一次**第 4 次驗收，驗收結論本身無誤；失效的是它的身分綁定——
它宣稱 digest 由「相對路徑」算出，實際卻是用**裸檔名**算的，檔名與自述的規則不一致。
第 5 次驗收的第 7 條據此判 FAIL。**舊檔保留、不修改、不刪除**：它自己宣告不可覆寫，
改它會破壞紀錄機制的契約，所以以新增方式取代。舊檔自此只作歷史，不得作為 gate 依據。

## 0. canonical digest 規則（本檔起固定）

subject digest ＝ 下列字串的 SHA-256：

* 每列為 `<sha256>` ＋ **兩個空格** ＋ **完整 repo-relative path**
* 依第 1 節表列順序
* 以 `\n` 連接
* **無尾換行**

檔名取該 digest 的**前 16 個 hex 字元**。三種曾出現過的變體實測值如下，
留在此處是為了讓下一輪能一眼確認自己算的是哪一種：

```
460e5a01bfd601ee0e562a0f73225130f80a50f215c02987acc6ec9d87f32d72  相對路徑、無尾換行  ← canonical
eaf5be8a3161a1a1a7b8e3613a84a2072aaad95550718aedf5a21621333ac548  相對路徑、有尾換行
f79e0b389b2455b013199222cbbf0ad830eeae511a106471ffb81a9a53d022b7  裸檔名、無尾換行    ← 舊檔誤用
```

## 1. 被驗物（驗收當下）

| 檔案 | SHA-256 |
|---|---|
| `skills/harness/evals/KNOWN-ISSUES.md` | `0c7bc8cc727b4655be07cbe2834a6a3fc179c6d6408ef25928f8b9cddcca7fac` |
| `skills/harness/evals/STATUS.md` | `793a1149d2678dc2ba6e8addd485e54d28ba31ec5f8806208fad234b3a66f068` |
| `skills/harness/evals/RECHECK-20260904-supersedes-closure.md` | `27236b5e2668bf6f080e33755e8c04124e7b882b6676ea4e6e767a9e9ecfd2bf` |

**subject digest**：`460e5a01bfd601ee0e562a0f73225130f80a50f215c02987acc6ec9d87f32d72`
（依第 0 節規則自上表重算得出）

HEAD：`b9cb8150a30be2b69b07a615e02f5ed8b9f21d2b`（未 commit）
working tree：52 筆、暫存區 0 筆
另有五個「宣稱未動」檔案同時鎖定：`CLOSURE-20260904-disposition.md`
`2eb828e2…`、`DE-01-RESULTS-20260904.md` `d391d657…`、`DE-01-PREFLIGHT.md` `e86c4fa1…`、
`judgment/SKILL.md` `10a88e48…`、`judgment/evals/fixtures.json` `8a6d99f4…`

## 2. 驗收契約

16 條，契約全文由實作者於 session scratchpad 提供，**未落檔進 repo**。
**因此契約本身的 hash 無法從 repo 重算——這是本紀錄的第一個 provenance 缺口。**
自第 5 次起契約與紀錄同批落檔（`verification-contracts/`），此缺口對第 5 次起不再存在。

## 3. 結果

**達到門檻**：無 FAIL；`UNVERIFIABLE` 僅第 16 條（契約預先列明的歷史 provenance 條件）。
**整體正式 verdict：`UNVERIFIABLE`。** 依門檻定義**不得記為 `PASS`**——
第 16 條先驗即為 `UNVERIFIABLE`，整體判定不可能是 `PASS`。

逐條：1–15 PASS，16 UNVERIFIABLE。第 16 條的理由：repo 內無 pre-cycle snapshot，
三個修改檔皆 untracked、`git log --all` 無紀錄，「本輪只改了哪些 byte」無法重建。

**本次修正不改動第 3 節的任何結論。** 失效的只有舊檔的身分綁定，不是驗收結果。

## 4. Verifier 識別

| 項目 | 值 |
|---|---|
| 角色 | fresh-context 唯讀驗收，新 session |
| session／task ID | **未取得** |
| 模型／執行時間 | **未取得** |
| 原始輸出 digest | **未取得**——原始輸出未被保存，本檔為轉錄 |

**無法證明 freshness。** 上述四項都取不到，因此本紀錄**不能證明**該次驗收確實在
一個未受實作者脈絡污染的 session 中執行。自第 5 次起，驗收契約要求 verifier
回報自身 session／task ID，並在無法提供時明白寫出；原始輸出 digest 改由呼叫端
於收到回覆後計算，不再要求 verifier 自算（自我指涉，無法計算）。

## 5. 本檔的證據力邊界

**本檔由實作者轉錄，只算 archive，不因落檔而取得獨立 provenance。**
它固定的是版本標記與逐條結論，讓任何人能用第 1 節的 hash 重跑第 3 節的檢查；
它**不是**第三方見證，也不能替代驗收本身。這與
`CLOSURE-20260904-disposition.md` §6 的自述同一標準。

第 0、1、3 節可機械重算；第 4 節完全依賴實作者自述，無任何佐證。
**本檔取代舊檔一事同樣是轉錄行為**：它證明的是「舊檔的 digest 與其自述規則不符」
這個可重算的事實，不因新增本檔而讓第 4 次驗收取得它本來沒有的 provenance。
