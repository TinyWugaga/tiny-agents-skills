# Targeted closure 契約 — 2026-09-04 FIX_FIXTURE ×8 + FIX_SKILL ×2

給**另一個 fresh-context session** 使用。執行者不得參與本次修改，也不得讀取本次修改的
推理過程；本檔已包含驗收所需的全部輸入。

## 1. 產出物與版本標記

repo：`/Users/tinywu/Desktop/Apps/tiny-agents-skills`
HEAD：`b9cb8150a30be2b69b07a615e02f5ed8b9f21d2b`（**未 commit**，working tree dirty）

因為 dirty，版本標記一律用檔案內容 SHA-256，不用 commit hash。

本次寫入的檔案共 **五個**，分成兩類：**四個 implementation artifacts**（驗收對象）
＋**一個獨立的 closure contract**（本檔，是驗收工具而非驗收對象）。

### 4 個 implementation artifacts（驗收對象）

| 檔案 | SHA-256（驗收時應相符） |
|---|---|
| `skills/discipline/judgment/SKILL.md` | `10a88e48388811d5bc957055ee960ae81b92fecc5bd3faaaeabdadd1ac36439e` |
| `skills/discipline/judgment/evals/fixtures.json` | `bee44a23678ff8742f6e277ed3ac5d55e09aa7ad915577d340e52439427a938d` |
| `skills/harness/evals/routing-fixtures.json` | `79d0997390f7f934b977b46d434570b97dd237e8953aeffcd639f29f4c8458c4` |
| `skills/harness/evals/DE-01-arms.md` | `a82429cb982229f65b0c5881bd32b6cae152dcedd155a8c967ec9cd2bbf50e51` |

### 1 個 closure contract（不是驗收對象）

`skills/harness/evals/CLOSURE-20260904.md` — 本檔。它在同一輪被寫入，但描述的是驗收
本身，**明確排除於 B1 的「未變」比對之外**：拿契約去證明契約沒變是循環論證。本檔不屬於
任何 identity domain，內容變動不影響任何 hash。

**除上述五個檔以外，任何檔案有差異即為 FAIL。**

## 2. 適用指示（以路徑提供，驗收者需自行讀取）

- `skills/harness/evals/KNOWN-ISSUES.md` — 逐筆 `required_action` 的來源，判準以此為準。
- `scripts/bundle-hash.sh` — hash domain 定義（`hash_schema v6`）。
- `skills/harness/evals/routing-fixtures.json` 的 `field_definitions` — fixture schema 語意。

## 3. 驗收條件（逐條可判定，共 16 條）

### A. 逐筆 KI 實作

| # | 條件 | 對象 |
|---|---|---|
| A1 | `judgment__positive-2` 的 `required_elements` 不再要求回應逐字出現 `PASS`／`FAIL`／`UNVERIFIABLE`，改為只要求「逐條三值判定 + 各自證據」，且明說用語不限 | KI-01 |
| A2 | 同一 fixture 的「整體判定優先序」條目改為描述優先序語意（最嚴重者為準），不綁定 `FAIL > UNVERIFIABLE > PASS` 這串字面 | KI-01 |
| A3 | `judgment__positive-4` 的 prompt 含待驗收產出物本文與逐條驗收條件，模型不需再向使用者索取輸入即可執行驗收 | KI-04 |
| A4 | `judgment__negative-1` 的 prompt 含 function 所在路徑，且該路徑在 `skills/harness/evals/seed/` 內真實存在 | KI-06 |
| A5 | `judgment__negative-1` 的 `forbidden_elements` 只禁「輸入齊備且無判斷點卻暫停」，明確把「缺必要輸入而索取輸入」排除在外 | KI-06 |
| A6 | `judgment__explicit_mention-2` 的 prompt 指定目標規則檔路徑，並含本次 diff | KI-10 |
| A7 | `judgment__explicit_mention-3` 的 prompt 含遷移計畫本文（目標、範圍、步驟、驗收條件、明確排除、被審版本） | KI-13 |
| A8 | `harness-routing__handoff-1` 的 prompt 提供待驗產出物路徑、適用指示路徑，以及 judgment 的逐條判定；該逐條判定即驗收條件，因此 fixture 不再與 `forbidden_elements`「由 dispatch 重新產生一套完成條件或驗收判準」互斥 | KI-17 |
| A9 | `harness-routing__judgment-absent-1` 的 prompt 含待核對的具體路徑 | KI-19 |
| A10 | `harness-routing__agent-unavailable-1` 的 prompt 含掃描目標與判準（範圍「全 repo／src/」本來就有） | KI-20 |
| A11 | `judgment/SKILL.md` 的 `description` 新增「互斥方案 + 選錯成本高 + 徵詢語氣」觸發面，且含至少一個徵詢語氣的具體例子 | KI-07 |
| A12 | `judgment/SKILL.md` 的「調高能力」段落新增規則，強制**在該節列出的調高能力訊號成立時**輸出能力升降／reasoning effort／推理耦合度的判斷。判準含否定面：該規則的作用範圍不得寫成「觸發本 skill 時」或任何會波及完成判斷、fresh-context 驗收、有界複查的措辭；**未新增其他 judgment 流程規則** | KI-09 |

A3～A10 的 prompt 內若引用 repo 路徑，該路徑必須在 `skills/harness/evals/seed/` 內真實存在，
或該檔內容已完整寫在 prompt 裡；兩者皆非即 FAIL。

### B. 邊界（不得發生的變更）

| # | 條件 |
|---|---|
| B1a | **有 identity domain 覆蓋、且有修改前 baseline 的檔案未變。** 對象：`judgment/references/**`（含 `bounded-review.md`）與 `judgment/scripts|assets|agents/**`、harness evaluator（`judge.py`／`score.py`／`record.py`）、runner（`run-suite.sh`／`run-fixture.sh`／`transport.py`／`manifest.py`）、`scripts/run-rescore.sh`、`dispatch/**`、`token-preflight/**`、`evals/seed/**`、`dispatch/evals/fixtures.json`。判法見下方「B1a 的驗證程序」 |
| B1b | **無 identity domain 覆蓋、也沒有修改前快照的檔案。** 對象：`skills/harness/evals/runs/**`（依 `bundle-hash.sh` 設計，`evals/runs/` 被每個 domain 明確排除），以及 `skills/harness/evals/` 下的其他說明文件（`STATUS.md`／`KNOWN-ISSUES.md`／`ACCEPTANCE-r2.md` 等）。**本條先驗地標為 `UNVERIFIABLE`**，除非驗收者能取得獨立於本次修改的可信 fingerprint（commit、封存 digest 或第三方快照）。可得的最強旁證只有 mtime：`find skills/harness/evals/runs skills/harness/evals -maxdepth 1 -type f -newermt "2026-09-04 00:00"` 應只列出 `DE-01-arms.md` 與 `CLOSURE-20260904.md`。**mtime 不等於內容未變**（可被保留或偽造），因此它把 B1b 降為「無反證」，不得升格為 PASS |
| B2 | 兩份 fixture 檔的 `schema_version` 仍為 `2.1` |
| B3 | `judgment__ambiguous-3` 的 `expected_trigger` 仍為 `true`，且其 prompt 未變 |
| B4 | 沒有任何 `NEEDS_EVIDENCE` 的 KI（KI-02／03／05／08／11／12／14／15／16／18）被實作或改判 |

#### B1a 的驗證程序

`judgment` 的 skill domain 涵蓋 `SKILL.md + references/** + scripts/** + assets/** + agents/**`
（見 `scripts/bundle-hash.sh` 的 `skill` mode allowlist）。因此不需要 `bounded-review.md`
的個別 SHA——修改前的 domain hash 就是它的 fingerprint：

1. 把 `skills/discipline/judgment/` 複製到 repo 外的暫存目錄。
2. 從副本的 `SKILL.md` 移除下方「本次對 SKILL.md 的兩處插入」全文（逐字，含換行）。
3. `sh scripts/bundle-hash.sh skill <副本路徑>`，結果應等於修改前 baseline
   `0c5a3deaec63cb3c39c1aea4b0b6417784a734d33b3b1ad3edf84334738de7ac`。
4. 相符即證明：該目錄下除了這兩處插入之外，**每一個 byte 都與修改前相同**。

其餘 B1a 對象由第 4 節的 domain hash 表直接判定（`evaluator`／`runner`／`rescore_runner`／
`dispatch_skill`／`token_preflight_skill` 應與修改前完全相同；`fixtures` 的變動應只來自
`routing-fixtures.json`，可用「把 `routing-fixtures.json` 換回原內容後重算」驗證）。

**本次對 `SKILL.md` 的兩處插入**（第 2 步要移除的就是這兩段，逐字）：

插入一（description，KI-07）：

```
  也用於在兩個以上互斥方案之間取捨、且選錯代價高的提問,包含以徵詢語氣提出時
  (如「照 A 方案還是不相容的 B 方案」「選錯會讓已有用戶必須重寫」「你覺得呢」)。
```

插入二（「調高能力」段落末，KI-09;前面有一個空行）：

```
本節任一調高能力訊號成立時,回應必須明確給出能力判定的其中一個結果:建議調高模型能力或
reasoning effort、維持現有能力、或當前環境不支援調整;並寫出判定依據的推理耦合度或
失敗軌跡。上述訊號成立時,即使該輪的主要問題是完成、暫停或換路,也不得略過這一項;
訊號都不成立時不需輸出這項判斷。
```

## 4. 允許的驗證方式（全部零成本，**不得起任何付費 session**）

```
python3 skills/harness/evals/{judge,score,record,transport}_selftest.py
sh scripts/hash-domain-selftest.sh
sh skills/harness/evals/run-suite.sh --dry-run
HARNESS_FX=../../discipline/judgment/evals/fixtures.json \
  sh skills/harness/evals/run-suite.sh --dry-run     # 需 cd 到 skills/harness/evals
sh scripts/bundle-hash.sh <mode> ...
git status --short ; git diff
```

預期值：四支 Python selftest 全通過；`hash-domain-selftest` 36 案例全通過；
default dry-run 18 筆、judgment dry-run 28 筆；每筆 dry-run 解碼後的 prompt 與 JSON 逐 byte 相同
（多行 prompt 不得被拆成多筆）。

domain hash 預期值（`hash_schema v6`）：

| domain | 修改前 | 修改後（應相符） |
|---|---|---|
| `evaluator` | `65bc6c07d058697cda34045637d3223568cf09626cc38b8385bac46d41157480` | 不變 |
| `runner` | `005ed2f71ac49a8b6747dd1c43f42c8ee67315a9fb3ec8ce395f921ccf6cee3c` | 不變 |
| `rescore_runner` | `2301a9daa8d90536e31b64f571767307e27a541fc369920bd882960354660b28` | 不變 |
| `dispatch_skill` | `2d6c0d99b1c4ca927e2c06c82e065c389fa6a6b8ddbc325b0e9bde9bc071c4fc` | 不變 |
| `token_preflight_skill` | `be72017e9fc7952e9a403cf90e951669496b08f4339938bc73a2829d44ef592a` | 不變 |
| `judgment_skill` | `0c5a3deaec63cb3c39c1aea4b0b6417784a734d33b3b1ad3edf84334738de7ac` | `a26b436888d81e7181199932590ca29f21e58ff9b8066eb792458e68ed5b3d47` |
| default `fixtures` | `a5413158ee621cebfcb595ef4562acc4fcc3d8ec1dd7f33763551122d1afe1c5` | `867d247699a03c32ae0a3892992931102fe26f24d76b25ce04b5f80b462175f0` |
| judgment `fixtures` | `c7559b7168d92ceba6304cefa78a234185b07e640eb51a179d87dd4d759de52c` | `d635c98e5d2959b3b15d72bdbdb5ffc40c6cc847b6da7d445e07602f3a75944c` |

`fixtures` 指令：
`sh scripts/bundle-hash.sh fixtures <該 suite 的 fixtures.json> skills/harness/evals/routing-fixtures.json skills/harness/evals/seed`

## 5. 明確排除範圍（超出者記入 backlog，不在本輪處理）

- **不驗證 routing／contract 是否會 PASS。** 那需要付費 subject／judge session，本輪禁止。
  任何「fixture 已修好所以會通過」的推論一律不得列為 finding 或 closure 依據。
- **不執行 DE-01 A/B**，也不評估 description 擴張的因果。
- **不改動任何 KI 的 Owner disposition**，不更新 KNOWN-ISSUES.md 的 gate 現況。
- 不評 `NEEDS_EVIDENCE` 十筆、不評步驟 F／G。
- 不做 commit、push、發布，或更新已安裝的 skill 副本。
- 不評論 fixture 的文風、措辭美感或行數；只判上表 16 條。

## 6. 環境限制

- working tree 在 HEAD 之外本來就有大量與本次無關的 dirty 變更（見 `git status --short`）。
  **這些必須原樣保留**，不得被「清理」。第 3 節 B1a／B1b 的比對對象是「本次修改前的 working tree」，
  不是 HEAD。
- `skills/harness/evals/runs/**` 是證據封存，唯讀。
- `skills/harness/evals/*.md` 說明文件不屬於任何 identity domain
  （`hash-domain-selftest.sh` 的「改 STATUS.md／KNOWN-ISSUES.md → none」案例是機械證據），
  因此新增 `DE-01-arms.md` 與本檔不改變任何 hash，也因此這兩個檔沒有 domain hash 可作為 fingerprint。

## 7. 回報格式

逐條回報 A1–A12、B1a、B1b、B2–B4 的 **PASS／FAIL／UNVERIFIABLE** 與可追溯證據（檔案路徑 + 行號或
指令輸出）。整體判定取最嚴重的單條結果（B1b 先驗即為 `UNVERIFIABLE`，因此整體判定至少是 `UNVERIFIABLE`，除非取得獨立 fingerprint）。回報後結束，**不進入修復或重新驗收循環**。
