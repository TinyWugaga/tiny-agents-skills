# DE-01 A/B 執行結果 — 2026-09-04

執行參數見 [`DE-01-PREFLIGHT.md`](DE-01-PREFLIGHT.md)。本檔只保存結果與逐筆判定。
**不更新任何 Owner disposition，不更新發布 gate**——那是 Owner 的裁決。

HEAD：`b9cb8150a30be2b69b07a615e02f5ed8b9f21d2b`（未 commit）｜`hash_schema` v6
成本：**USD 7.040700** ＝ DE-01 A/B `6.259000`（24 subject ＋ 6 preflight ＋ 24 judge）
＋ KI-07 continuation `0.781700`（3 subject ＋ 3 preflight ＋ 3 judge）＝ **63 paid calls**

## 1. Run record

| run ID | run_status | evidence_class | cost | judgment_skill | 秒 |
|---|---|---|---|---|---|
| `de01-armB-r1` | FAIL | contract_evidence | $1.1965 | `a26b4368…` | 514 |
| `de01-armB-r2` | FAIL | contract_evidence | $1.0680 | `a26b4368…` | 478 |
| `de01-armB-r3` | FAIL | contract_evidence | $1.0845 | `a26b4368…` | 534 |
| `de01-armA-r1` | FAIL | contract_evidence | $0.9586 | `6e96c802…` | 416 |
| `de01-armA-r2` | FAIL | contract_evidence | $1.0503 | `6e96c802…` | 497 |
| `de01-armA-r3` | FAIL | contract_evidence | $0.9011 | `6e96c802…` | 390 |

`run_status FAIL` 來自 routing／contract assertion，**不是無效執行**：六輪
`invalid_reason` 皆為 `null`、`cli_failed` 皆為 0、provenance 無 problem。

六輪共用同一組凍結 domain（`fixtures 7a1b54c1…`、`evaluator 65bc6c07…`、
`runner 005ed2f7…`、`dispatch_skill 2d6c0d99…`、`token_preflight_skill be72017e…`），
**唯一有兩個值的是 `judgment_skill`**——協定第 1 條滿足。

## 2. Routing 觸發矩陣

| fixture | armB-r1 | armB-r2 | armB-r3 | armA-r1 | armA-r2 | armA-r3 |
|---|---|---|---|---|---|---|
| `positive-1` | . | . | P | . | . | . |
| `positive-2` | P | P | P | . | . | P |
| `positive-4` | P | P | P | P | . | P |
| `positive-5` | . | . | . | . | . | . |

（P ＝ routing 觸發，. ＝ 未觸發；arm A ＝ pre-bounded description，arm B ＝ 現行定稿）

## 3. 逐筆判定（`KNOWN-ISSUES.md:104–113`）

門檻：同一筆 fixture **arm A 至少 2/3 且 arm B 至多 1/3** 才支持 `description_dilution`。

| fixture | KI | arm A | arm B | 門檻 | 結論 |
|---|---|---|---|---|---|
| `positive-1` | KI-14 | 0/3 | 1/3 | 不成立 | 不支持 `description_dilution` |
| `positive-2` | KI-03 | 1/3 | 3/3 | 不成立 | 不支持 `description_dilution` |
| `positive-4` | KI-05 | 2/3 | 3/3 | 不成立 | 不支持 `description_dilution` |
| `positive-5` | KI-15 | 0/3 | 0/3 | 不成立 | 不支持 `description_dilution` |

依協定第 3 條，四筆都應由 Owner 改判為 `stochastic_known_issue` 或 `skill_defect`。
**本檔不做這個改判。**

### 3.1 判定用語的邊界

協定只提供**支持** dilution 的門檻，沒有提供**反駁**的門檻。因此本輪能說的只有
「四筆皆不支持 `description_dilution`，需改判」。

以下是觀測記錄，不是結論：`positive-1`／`positive-2`／`positive-4` 三筆的 arm B
觸發次數高於 arm A。這與 dilution 預測的方向相反，但每格 n=3、且 routing 本身已知
會翻轉（KI-11／KI-16 的 `stochastic_known_issue`），**不足以宣稱命題被反駁**。

`positive-5` 兩個 arm 都是 0/3，只能說**本輪未觀測到 description 造成的差異**，
不能推論它與 description 無關——兩 arm 同為 0 也相容於「兩份 description 都不觸發」。

Batch-11 的單次 A/B 曾在 `positive-1` 觀測到「舊觸發、新未觸發」，本輪 3×2 未重現該方向。
這與協定第 4 條「單次 observation 不成立」一致。

## 4. Contract（judge）觀測，供 KI-02 等下游參考

| fixture | armB-r1 | armB-r2 | armB-r3 | armA-r1 | armA-r2 | armA-r3 |
|---|---|---|---|---|---|---|
| `positive-1` | PASS | PASS | PASS | PASS | PASS | PASS |
| `positive-2` | FAIL | FAIL | FAIL | FAIL | FAIL | FAIL |
| `positive-4` | PASS | FAIL | FAIL | PASS | FAIL | FAIL |
| `positive-5` | PASS | PASS | FAIL | PASS | PASS | PASS |

`positive-2` 的 contract 在 routing 觸發的三輪 arm B 全部 FAIL——KI-02 的待證命題
（SKILL.md 是否缺「何時必須開獨立 context」的規則）現在有了 routing PASS 的驗收型
observation。**本檔不對 KI-02 下判定**，只記錄證據已存在。

## 5. KI-07 continuation — `ambiguous-1` arm B ×3

於 2026-09-04 補跑完成，**與 DE-01 cycle 同一批**：8 個 identity domain 中 7 個逐一相同
（`judgment_skill a26b4368…`、`execution_context e7beb6ed…`、`evaluation_context 64996161…`、
`evaluator`、`runner`、`dispatch_skill`、`token_preflight_skill`），
唯一不同的是 `fixtures`（`dbbb4786…`，只含 `ambiguous-1` 一筆）——這正是設計上該有的差異。

| run ID | run_status | evidence_class | routing | contract | cost | 秒 |
|---|---|---|---|---|---|---|
| `ki07-armB-r1` | PASS | contract_evidence | PASS | PASS | $0.3475 | 66 |
| `ki07-armB-r2` | PASS | contract_evidence | PASS | PASS | $0.2152 | 71 |
| `ki07-armB-r3` | PASS | contract_evidence | PASS | PASS | $0.2190 | 70 |

三輪 `invalid_reason` 皆 `null`、`cli_failed` 皆 0、provenance 無 problem、無 domain drift。

**結果：routing 3/3 觸發，contract 3/3 通過。**

KI-07 的 `FIX_SKILL` required_action 是「改 SKILL.md／description；改完重跑該筆」。
description 已於本 cycle 定稿（`judgment_skill a26b4368…`），`ambiguous-1` 已重跑並全數通過。
**本檔不改 Owner disposition**——是否據此把 KI-07 記為 closed 是 Owner 的裁決。
同一份證據也適用 KI-08（`ambiguous-1` 的 `routing→contract` 下游）：
routing 觸發後 contract 3/3 PASS，即該下游本輪未觀測到缺陷。

對照 batch-12：`ambiguous-1` 當時 routing 未觸發（KI-07 的 `skill_defect` 依據）。
本輪 3/3 觸發。兩輪之間 `judgment_skill` 由 `0c5a3dea…` 變為 `a26b4368…`
（description 新增「互斥方案 ＋ 選錯成本高 ＋ 徵詢語氣」觸發面），
`fixtures` 亦不同（縮減集），**因此這是重跑通過的證據，不是受控 A/B**。

## 6. 證據力缺口（誠實記錄，不是可修的 bug）

本輪已無未完成的執行項目。以下是證據力缺口，記錄而不掩蓋：

| 項目 | 狀態 |
|---|---|
| fresh-context verifier 的 V1–V12 報告 | **無可追溯產物。** 該 verifier 以 subagent 執行，報告只存在於實作者的對話紀錄，repo 內沒有留下版本 hash 或輸出檔。事後把它抄進 repo 也**不能**補回獨立性——那仍是實作者自述。下一輪應要求 verifier 直接把報告寫成 repo 內檔案並附自身版本標記 |
| 總耗時 62 分鐘 | **無封存紀錄。** run record 只能證明付費區段約 48.6 分鐘（六輪 duration_s 合計 2829 秒＋輪次間隔）。12:28:37 的起點只有實作者自述 |

## 7. 邊界

未 commit、未 push、未發布、未更新已安裝的 skill 副本。
`KNOWN-ISSUES.md`（`401b7f63…`）、`STATUS.md`（`b16261c8…`）、
`CLOSURE-20260904.md`（`e77c9502…`）、`judgment/SKILL.md`（`10a88e48…`）、
`judgment/evals/fixtures.json`（`8a6d99f4…`）本輪皆未變。
arm A 全程在 repo 外完整副本執行，真實 working tree 沒有發生過 swap。
