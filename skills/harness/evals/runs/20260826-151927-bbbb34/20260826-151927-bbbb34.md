> **本輪已作廢（SUPERSEDED）。** harness 有 fail-open 缺陷、raw trace 未保存、
> 且只評了 routing assertion 就被摘要成「17/17 PASS」。結果不可作為 release gate 憑據。
> 見同目錄 `.json` 的 `superseded_reason`。

# harness evals run — 20260826-151927-bbbb34

- Claude Code: `2.1.221 (Claude Code)`
- 記錄時間: 2026-08-26T15:25:57.322067+08:00
- hash 方法: `skills/harness/dispatch/scripts/bundle-hash.sh <目錄> [skill|suite]` — 路徑無關(相對路徑+內容 hash 組合)，排除 evals/runs/
- **hash baseline 已失效（INVALIDATED）**：本輪以舊演算法（副檔名比對）計算；Batch 2 改為 allowlist 並納入 `scripts/**` 後，同一份內容會算出不同值。下表僅為歷史觀測，**不得拿來對應任何現存 bundle**，也不得以現行腳本重算後覆寫。

## Hashes

| 項目 | SHA-256 |
|---|---|
| `dispatch_bundle` | `2bcc9b7d8b2e14301b91d71ae1abba9b003230f82a66ed2d9e9cdf31bcdcf98a` |
| `judgment_bundle` | `5b043ab762b7c610ae885225532104248268e70b6fb3bf3652d94816f24a18b0` |
| `harness_test_suite` | `a4e26898a144d78aef013dc27c821bed10b667d38f3c28a87ff5683aa321277a` |
| `claude_md` | `7b26b19dd35d91810e24a64138f93c69bd4b55c0351476c1aead4cbbf811859a` |
| `settings_json` | `79a4ff04b0f3d5025891978904bf8b18f908b082db349002c8ad194a7a9907dc` |

## 狀態

**PENDING — fixture 實跑需在全新 session 逐筆輸入 prompt 測 implicit invocation，非互動式 session 無法代跑**

## 怎麼跑（需互動式的全新 session）

每筆開一個**全新** session，逐字輸入 `prompt` 欄，觀察**哪個 skill 被自動載入**
（測 implicit invocation，不要打 `/dispatch`），把結果填回 `run-record.json` 的
`actual_triggered_skill` 與 `result`。

判定規則：
- `dispatch_fixtures`：`expected_trigger` 為 true 時 dispatch 必須被載入；為 false 時 dispatch 必須**沒有**被載入。
- `routing_fixtures`：以 `expected_route` 逐一比對，map 中每個 skill 的 true/false 都要吻合。
- 帶 `[環境:…]` 前綴的 fixture 需先按該前綴調整環境（暫時移除對應 symlink 或停用 Agent tool），跑完復原。

## 待跑清單

### dispatch (9 筆)

| id | category | expected | prompt |
|---|---|---|---|
| `positive-1` | positive | `True` | 幫我派個 agent 掃一下整個 repo,找出所有還在用舊版 API 的地方 |
| `positive-2` | positive | `True` | 這個子任務要用 sonnet 還是 opus?effort 開多少比較合適? |
| `positive-3` | positive | `True` | 幫我寫一份派工 prompt,要讓 subagent 去重構 useCart 這個 hook |
| `positive-4` | positive | `True` | 這件事我要不要開三個 agent 平行跑? |
| `negative-1` | negative | `False` | 幫我把這個 function 改成 async |
| `negative-2` | negative | `False` | 這個 bug 我連續兩輪都修不好,怎麼辦 |
| `negative-3` | negative | `False` | 幫我驗收這份文件有沒有照規格寫 |
| `ambiguous-1` | ambiguous | `True` | 這個任務有點大,要不要拆出去跑? |
| `explicit-mention-1` | explicit_mention | `True` | 跑一下 dispatch |

### routing (8 筆)

| id | category | expected | prompt |
|---|---|---|---|
| `handoff-1` | positive | `{"judgment": false, "dispatch": true}` | judgment 已經判定這份規則檔需要 fresh-context 驗收了,幫我把驗收派出去 |
| `boundary-1` | negative | `{"judgment": true, "dispatch": false}` | 我把這個功能做完了,幫我跟使用者說已經完成 |
| `boundary-2` | negative | `{"judgment": true, "dispatch": false}` | 同樣的錯誤我改兩輪了還是失敗,是不是該換更強的模型 |
| `dispatch-absent-1` | positive | `{"judgment": false, "dispatch": false}` | [環境:dispatch skill 未安裝] 幫我派一個 subagent 去掃 src/ 底下所有的 TODO |
| `dispatch-absent-2` | negative | `{"judgment": false, "dispatch": false}` | [環境:dispatch skill 未安裝] 幫我看看這個 hook 為什麼會重複觸發 |
| `judgment-absent-1` | positive | `{"judgment": false, "dispatch": false}` | [環境:judgment skill 未安裝] 這個多步驟重構我做完了,可以宣告完成了嗎 |
| `agent-unavailable-1` | positive | `{"judgment": false, "dispatch": true}` | [環境:Agent tool 被停用或權限被拒] 幫我派個 agent 去做全 repo 掃描 |
| `ambiguous-1` | ambiguous | `{"judgment": true, "dispatch": false}` | 這份產出品質夠了嗎?要不要找人再看一次? |
