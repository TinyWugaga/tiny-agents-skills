# harness run 20260830-000907-9e — findings

## 範圍

本輪是 `dispatch__positive-3` 的**單筆 targeted regression validation**，不是完整 suite。目的只在驗證 `dispatch` 撰寫重構派工 prompt 時明列載入 `references/templates.md` 後，先前 9d 的 contract FAIL 是否仍會出現。其他 fixture 本輪未執行，對它們沒有任何新結論。

## 結果

| fixture | routing | contract |
|---|---|---|
| `dispatch__positive-3` | PASS | PASS |

subject 成功觸發 `dispatch`；judge 的 3 條 required 與 2 條 forbidden 逐字對應原 fixture，五條 verdict 全為 PASS，`overall` 重算為 PASS。

## 與 9d 的對照

| run | dispatch bundle | routing | contract |
|---|---|---|---|
| `20260829-225157-9d` | `6c9d922dcdac30fa500201504de58d030dc35d7522db802be06756409e7e2149` | PASS | FAIL |
| `20260830-000907-9e` | `fd139f25b7e5745df7c5ab6aa1d20629e40edcb8a807f5277c11acf433eddcae` | PASS | PASS |

9e 的結果支持「撰寫派工 prompt 前明確載入並填寫模板」這項修正有效；但這是單次實跑 observation，不足以單獨證明統計穩定性或排除 session 變異。9d 的 FAIL 紀錄保留且未被覆寫。

## 成本

三項原始精確值（type=observation，metric=session 自報 `total_cost_usd`，unit=USD）：

| 來源 | USD |
|---|---|
| preflight | `0.038791` |
| `dispatch__positive-3` subject | `0.19784549999999998` |
| `dispatch__positive-3` judge | `0.0729364` |
| **精確總和** | **`0.3095729`** |

正式 run record 的 `cost.total_usd` 為 `0.3095`。差額 `0.0000729` 來自 `record.py` 先將 subject 與 judge 成本各自四捨五入到四位，再計算總和；原始精確值以封存 trace 與 judge JSON 為準。

本次收尾新增成本為 **USD 0**：只封存既有證據並產生 record，未啟動任何 session。

## 限制

- 本輪沒有重跑完整 harness suite。
- `score.py` 對失敗 `Skill` 呼叫的分支仍只有合成自測覆蓋；本輪真實 trace 的 `Skill(dispatch)` 呼叫成功，`attempted_failed=[]`。
- `duration_s` 維持 `null`；未以檔案 mtime 代理真實執行時間。
