# harness run 20260829-225157-9d — findings

## 範圍

本輪是**三筆 targeted partial validation**，不是完整 18 筆 suite。只涵蓋 `harness-routing__dispatch-absent-1`、`harness-routing__ambiguous-1`、`dispatch__positive-3` 三筆，其餘 fixture 本輪未執行，對它們沒有任何結論。

## 結果

| fixture | routing | contract |
|---|---|---|
| `harness-routing__dispatch-absent-1` | PASS | PASS |
| `harness-routing__ambiguous-1` | PASS | PASS |
| `dispatch__positive-3` | PASS | **FAIL** |

三筆 routing 全部 PASS。前兩筆 contract PASS；`dispatch__positive-3` contract FAIL。

### `dispatch__positive-3` 的 FAIL 原因

> 派工單雖有目標、範圍、驗收條件、回報格式與不變量，但沒有明確要求既有測試不得修改或刪除。

judge 記錄的原文依據：

- **element**：派工單含三件套,且註明不變量與既有測試不得修改
  - **evidence**：三件套齊備(目標/範圍/驗收條件+回報格式),不變量有註明(「useCart(userId: string) 的入參簽名維持不變」)。但全篇未出現「既有測試不得修改/不得刪除」這類約束;範圍段只寫「只動 src/hooks/useCart.ts 及必要設定檔」,驗收第 4 點只要求新增 useCart.test.ts,沒有任何一句限制既有測試不得改動。該行為未實際出現在回應裡。

這是**有效觀測**，不是環境問題，不修到綠。

## 成本

七項原始精確值（type=observation，metric=session 自報 total_cost_usd，unit=USD）：

| 來源 | USD |
|---|---|
| preflight | `0.0219256` |
| `harness-routing__dispatch-absent-1` subject | `0.17938129999999997` |
| `harness-routing__dispatch-absent-1` judge | `0.0569584` |
| `harness-routing__ambiguous-1` subject | `0.0508722` |
| `harness-routing__ambiguous-1` judge | `0.051903399999999995` |
| `dispatch__positive-3` subject | `0.1193148` |
| `dispatch__positive-3` judge | `0.078688` |
| **精確總和** | **`0.5590437`** |

正式 run record 的 `cost.total_usd` 顯示 `0.559`（JSON 不保留尾零，即 `0.5590`），與精確總和差 `0.0000437`。原因是 `record.py` 先把 `subject_usd` 與 `judge_usd` 各自 `round(..., 4)`（`0.3495683 → 0.3496`、`0.1875498 → 0.1875`），再與未取整的 `preflight_usd` 相加後再 `round(..., 4)`。差額來自中間取整，不是量測差異。以本檔的七項原始值為準。

本輪新增成本為 **USD 0**：只封存既有證據，未執行任何付費 session。

## 覆蓋缺口

`harness-routing__dispatch-absent-1` 的真實 trace 沒有失敗的 `Skill(dispatch)` 呼叫，`attempted_failed=[]`。9d 對 `score.py` 的修正中，「失敗的 `Skill` 呼叫不計入觸發」這條分支**仍只有合成自測覆蓋**，未被任何真實 session 資料驗證。不得記為已由真實資料覆蓋。

## 修正候選（本輪不實作、不重跑）

讓 `dispatch` 產生派工單時可靠地明寫「既有測試不得修改或刪除」。本輪不改 SKILL.md、不改 fixture、不重跑，留待後續批次決策。

